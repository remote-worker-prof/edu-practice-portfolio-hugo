# Учебные workflow (не для агентов)

Этот каталог содержит **учебные примеры** workflows, чтобы разбираться в структуре CNCF Serverless Workflow. Эти файлы **не используются** агентами и не подключены к автоматизации проекта.

Важно: примеры в этом каталоге выполнены в **классическом формате v0.8 (states)**. А рабочие агентные файлы в корне репозитория уже переведены на **Serverless Workflow DSL (document/do)**.

## Список файлов

1. `project-linear.cncf.yaml` — линейный процесс: подготовка → контент → сборка → деплой.
2. `project-parallel.cncf.yaml` — параллельные ветки (например, линт и сборка) с объединением.
3. `project-release.cncf.yaml` — выпуск релиза с ручной проверкой и финальным деплоем.
4. `state-vs-dsl.md` — сравнение классического state‑формата v0.8 и нового DSL.

## Как читать примеры

- `specVersion: "0.8"` — версия спецификации Serverless Workflow.
- `states` — шаги процесса.
- `actions` → `functionRef` — реальные действия (команды) в виде абстракций.
- `transition` — переход к следующему шагу.
- `end: true` — завершение workflow.

Все команды в примерах обозначены как `shell:` и являются **шаблонными**.
